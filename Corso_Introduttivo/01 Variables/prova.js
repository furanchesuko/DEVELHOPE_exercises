// Dichiaro le variabili e i valori
let isSunnyDay = true;
let isHomeworkCompleted = false;

//Dichiaro la variabile di condizione
let result = isSunnyDay && isHomeworkCompleted ? 'Jesse can goes out to play' : `Jesse stays home`;

console.log(result);
//in questo caso il print sarà 'Jesse stays home' in quanto la con2 è false.